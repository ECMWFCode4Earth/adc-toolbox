name: adc-toolbox
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.8.6
  - ipykernel
  - anaconda
  - cartopy
  - pip
  - pip:
    - -r requirements.txt
prefix: /opt/conda/envs/adc-toolbox
